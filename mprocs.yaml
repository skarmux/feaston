procs:
  editor:
    shell: "nvim ."
  cargo:
    shell: "cargo watch -x run"
  tailwindcss:
    shell: "tailwindcss -i styles/tailwind.css -o www/assets/main.css --minify --watch"
# hide_keymap_window: true
